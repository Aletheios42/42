// #include "../Inc/fractol.h"
//
// void hook_handler(t_mlx *mlx) {
//   // mlx_key_hook(mlx->win, hook_key_handler, mlx);
//   // mlx_mouse_hook(mlx->win, hook_mouse_handler, mlx);
//   // mlx_hook(mlx->win, ON_DESTROY, 0, window_close_hook, mlx);
//   // if (mlx->setting->title == "Julia")
//   //   mlx_hook(mlx->win, ON_MOUSEMOVE, 0, check_mouse_pos, mlx);
// }
